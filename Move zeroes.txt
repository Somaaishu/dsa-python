🔥 Problem

Move all 0s to the end while keeping the order of non-zero elements same.

Example:

Input:  [0,1,0,3,12]
Output: [1,3,12,0,0]

🧠 Logic

👉 Use Two Pointers

✅ j → position to place next non-zero
✅ Traverse array → when non-zero found, swap with j

⭐ Idea:

Non-zero → move forward  
Zero → automatically goes back

🧠 Memory Trick

👉 “Non-zero left, zero right.”
👉 Think Two Pointer Technique

⏱️ Complexity

✅ Time: O(n) → single traversal
✅ Space: O(1) → in-place (no extra array)

✅ Code (Best Short Version)
class Solution:

    def moveZeroes(self, nums):

        j = 0
        for i in range(len(nums)):
            if nums[i] != 0:
                nums[i], nums[j] = nums[j], nums[i]
                j += 1

        return nums


def main():

    nums = [0,1,0,3,12]

    obj = Solution()
    print(obj.moveZeroes(nums))


if __name__ == "__main__":
    main()

⭐ Interview One-Line Answer

“Using two pointers, I move non-zero elements forward while maintaining order. Time O(n), space O(1).”

🚨 Recognition Trick

If question says:

👉 Move zeros
👉 Maintain order
👉 Do it in-place

💡 Immediately think:

👉 Two Pointers